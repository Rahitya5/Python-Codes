def remove_duplicates_unsorted(arr):
    seen=set()
    unique_arr=[]
    for num in arr:
        if num not in seen:
            seen.add(num)
            unique_arr.append(num)
    return unique_arr
arr=[4,5,6,4,5,7,8]
print(remove_duplicates_unsorted(arr))
